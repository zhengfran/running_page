# Keep published history append-only

Once a Garmin Run is archived and published, routine synchronization does not rewrite or delete it when Garmin Connect later changes. This preserves a stable personal history and an immutable source archive; genuine mistakes are handled as explicit, reviewable Corrections with versioned replacements instead of silent provider-driven mutation.
